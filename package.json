{
  "name": "scintilla-run-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:e2e": "playwright test",
    "test:puppeteer": "node --test \"tests/puppeteer/*.test.mjs\""
  },
  "dependencies": {
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "puppeteer": "^24.0.0"
  }
}
